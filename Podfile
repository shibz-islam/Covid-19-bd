# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BD-Sta-Viz' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
    pod 'GoogleMaps'
    pod 'SwiftCSV'
    pod 'Charts'
    pod 'SwiftyJSON', '~> 4.0'
    pod 'SwiftKeychainWrapper'
    pod 'SideMenu'
    pod 'SCLAlertView'
    pod 'SwiftLocation', '~> 4.0'

  # Pods for BD-Sta-Viz

end
